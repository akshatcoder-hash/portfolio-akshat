---
import { IHeroProps } from "@types";

const { name, about, intro } = Astro.props as IHeroProps;
---

<section>
  <p class="dark:text-light mt-[110px]">gm ☀️ frens! it's</p>
  <h1>{name}</h1>
  <p class="dark:text-light text-lg mb-16">
    {about}
    <br>
    {intro}
  </p>
</section>
